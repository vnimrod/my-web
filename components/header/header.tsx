import React from 'react';
import { NavBar } from '../nav-bar/nav-bar';
import styles from './header.module.scss';

export const Header = () => {
  return (
    <div>
      <NavBar />
    </div>
  );
};
